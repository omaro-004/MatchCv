<style>
    .evt-form-layout {
        display: grid;
        grid-template-columns: 1.4fr 1fr;
        gap: 20px;
        align-items: start;
    }
    @media (max-width: 900px) {
        .evt-form-layout { grid-template-columns: 1fr; }
    }
    .evt-form-card { padding: 28px; }

    /* ── Toggle "en ligne / présentiel", même famille visuelle que Face ID ── */
    .evt-online-toggle {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 14px 16px;
        border: 1.5px solid var(--border);
        border-radius: var(--radius-md);
        background: var(--surface-2);
        margin-bottom: 16px;
    }
    .evt-switch { position: relative; display: inline-block; width: 46px; height: 26px; flex-shrink: 0; cursor: pointer; }
    .evt-switch input { opacity: 0; width: 0; height: 0; }
    .evt-switch-slider {
        position: absolute; inset: 0;
        background: var(--border);
        border-radius: 999px;
        transition: background .25s;
        display: block;
    }
    .evt-switch-slider.checked { background: var(--accent); }
    .evt-switch-knob {
        position: absolute; top: 3px; left: 3px;
        width: 20px; height: 20px;
        background: #fff;
        border-radius: 50%;
        box-shadow: 0 1px 4px rgba(0,0,0,.2);
        transition: transform .25s;
    }
    .evt-switch-slider.checked .evt-switch-knob { transform: translateX(20px); }
    /* masque la case à cocher native, pilotée par le slider stylé */
    #evenement_isOnline, #evenement_isOnline_input { position: absolute; opacity: 0; pointer-events: none; }

    /* ── Carte d'aperçu live ── */
    .evt-preview-card {
        position: sticky;
        top: 84px;
        background: linear-gradient(160deg, #12141f 0%, #1e2130 100%);
        border-radius: var(--radius-lg);
        padding: 24px;
        color: #e2e8f0;
        box-shadow: var(--shadow-lg);
    }
    .evt-preview-label {
        font-size: .7rem;
        text-transform: uppercase;
        letter-spacing: .08em;
        color: #818cf8;
        font-weight: 700;
        margin-bottom: 14px;
    }
    .evt-preview-badge {
        display: inline-block;
        font-size: .76rem;
        font-weight: 600;
        background: rgba(255,255,255,.08);
        padding: 4px 10px;
        border-radius: 999px;
        margin-bottom: 12px;
    }
    .evt-preview-title { font-size: 1.15rem; font-weight: 800; margin-bottom: 8px; line-height: 1.3; }
    .evt-preview-meta { font-size: .78rem; color: #94a3b8; margin-bottom: 14px; line-height: 1.6; }
    .evt-preview-desc { font-size: .85rem; color: #cbd5e1; line-height: 1.6; white-space: pre-line; }
</style>
